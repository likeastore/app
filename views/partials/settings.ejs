<div class="settings-page">

	<div class="userinfo-wrap">
		<div class="main-title"><i class="font-icon settings-icon"></i>{{title}}</div>
		<div class="info-item">{{user.displayName || user.name}} &middot; {{user.email}}</div>
		<div class="info-item date">*registered on {{user.registered|date: 'fullDate'}}</div>

		<div class="network-error-stripe" ng-show="user.warning">
			<i data-icon="E" class="icon"></i>
			<span class="message">Our robot reports the issue with one of your networks. Please help him by simply re-enabling the network.</span>
		</div>
	</div>

	<ul class="networks-wrap">
		<li class="network facebook">
			<div class="title">Facebook</div>
			<div class="desc">Sync all facebook likes and links.</div>
			<div toggle-switcher="facebook" model="networks"></div>
		</li>
		<li class="network twitter">
			<div class="title">Twitter</div>
			<div class="desc">Sync all tweets that you've favorited.</div>
			<div toggle-switcher="twitter" model="networks"></div>
		</li>
		<li class="network github">
			<div class="title">Github</div>
			<div class="desc">Sync all repositories that you've starred.</div>
			<div toggle-switcher="github" model="networks"></div>
		</li>
		<li class="network gist">
			<div class="title">Gist</div>
			<div class="desc">Sync all gists that you've starred.</div>
			<div toggle-switcher="gist" model="networks"></div>
		</li>
		<li class="network stackoverflow">
			<div class="title">Stackoverflow</div>
			<div class="desc">Sync all questions that you've favorited.</div>
			<div toggle-switcher="stackoverflow" model="networks"></div>
		</li>
	</ul>

	<div class="danger-wrap clearfix">
		<div class="main-title"><i class="font-icon delete-icon"></i>Delete account</div>
		<div class="info-item left">Once you delete an account, there is no going back.<br> This action <b>CANNOT</b> be undone and will delete all likes permanently.</div>
		<button type="button" class="btn red-btn delete-btn right" ng-dialog="deleteAccountDialog" ng-dialog-class="lsd-theme-default delete-user-dialog">Delete my account</button>

		<script type="text/ng-template" id="deleteAccountDialog">
			<div ng-controller="deleteUserController">
				<h2>Delete account?</h2>
				<div class="tip">Unexpected bad things will happen unless you are not ABSOLUTELY sure.</div>
				<button type="button" ng-click="goBack()" class="btn green-btn">No, thanks</button>
				<button type="button" ng-click="deleteAccount()" class="btn red-btn">Yes, please</button>
			</div>
		</script>
	</div>
</div>